
# Core framework
rasa-pro==3.9.20

# For loading .env files locally (non-optional for your use case)
python-dotenv>=1.0.0

# If you're calling OpenAI directly from Python code
openai>=1.0.0

# If you use pytest for custom action / unit tests
pytest>=8.0.0